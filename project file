#include <stdio.h>
#include <stdbool.h>
#include <string.h>
#define MAX_BOOKS 50
#define MAX_MEMBERS 30
#define MAX_BORROW 150
int countBooks = 0;
int memberID;
int booksList_ID[MAX_BOOKS];
int booksList_Copies[MAX_BOOKS];
int Members[MAX_MEMBERS];

struct books
{
  int bookID;
  int bookCopies;
};
struct borrowedBooks
{
  int bookID;
  int memberBorrowed;
  int Date[];
};
int inputBooks();
int main()
{
  inputBooks();
  printf("%d", booksList_Copies[0]);
}
int inputBooks()
{
  printf("Books:\n");

  struct books book;
  while (7)
  {
    int books_ID;
    int books_Copies;
    if (scanf("%d %d", &books_ID, &books_Copies) == EOF)
      break;
    booksList_ID[countBooks] = books_ID;
    booksList_Copies[countBooks] = books_Copies;
    countBooks++;
  }
}

int inputMembers()
{
    printf("Members:\n");
    int members;
    
}
